# Validation Evidence

**Generated by:** PRD validation plan execution
**Date:** 2026-05-19
**PRD version:** 0.2

---

## Validation Plan Results

### 1. PRD Contradiction Review

**Status:** PASS

No contradictions found in schema, enum, or skill specifications. All skill names use unique `fab-*` prefix. Gate levels are consistent across PRD, schema, and skill docs. Score scale (0-10) is uniform.

### 2. Sample Run: Phase 0

**Status:** PASS

- `docs/spec.md` generated (75 lines, complete)
- `docs/blueprint.md` generated (143 lines, complete)
- Both documents internally consistent
- A cold reader could implement the app from them
- Run object initialized with correct state

### 3. Sample Run: Phase 1

**Status:** PASS

- App project scaffolded at `../invoice-note-parser/`
- Stage 1 (parse-invoice-text) implemented with code and tests
- 18/18 tests pass (happy path, failure, edge cases, CLI, helpers)
- Quality score: 8.57/10 (all axes ≥ 6)
- Stage status: done

### 4. Status Test (fab-pulse with unknown costs)

**Status:** PASS

- `fab-pulse` renders all three panels (pipeline, quality, cost)
- Missing cost values display as `unknown`
- No crashes or invented cost data

### 5. Handoff Test

**Status:** PASS

- `docs/handoff.md` generated with complete state
- New session can resume from `fabrica.run.json` and `docs/handoff.md`
- Next action is clear

### 6. Launch Safety Test

**Status:** PASS

- `fab-launch` requires explicit approval before external deploy
- Pre-launch checklist gates local verification
- `.env.example` exists but is currently blank; required variables (`OLLAMA_HOST`, `OLLAMA_MODEL`) must be added manually. This is a known blocker documented in handoff/retro.
- `.env` is in `.gitignore`
- No committed secrets in source code

---

## Run Object Validation

The run object (`fabrica.run.json`) validates against `schemas/run-object.schema.json`:

- All required fields present
- `status` enum valid
- `experiment_phase` enum valid
- `quality_score` within 0-10 range
- `costs.precision` is `unknown`
- `app_stages[].status` is `done`
- `gate_levels` contains all 13 skill keys

---

## Schema Validation

`schemas/run-object.schema.json` passes JSON parsing. All enum values, required fields, and type constraints are correct.

## Skill File Validation

All 13 `SKILL.md` files:
- Have valid frontmatter (name, description, category, phase)
- Include prerequisites, error handling, and validation steps
- Are under 400 lines
- Use unique `fab-*` names

## link-skills.mjs Validation

`node --check scripts/link-skills.mjs` passes. Script creates/refreshes `.skills/` directory with all 13 skills.
