# Archived design records

These documents are **historical**. They are not the live workflow.

Current source of truth:

- Skill inventory and gates: `skills/manifest.json`
- Run-object schema: `schemas/run-object.schema.json`
- Semantic validation: `scripts/validate-run.mjs`
- Operator pathways: `docs/STATE_MACHINE.md`
- Executable checks: `npm run check`

Pre-1.0 skill ids (`fab-intake`, `fab-blueprint`, …) may appear here. Canonical ids are in `skills/manifest.json` and the README inventory table.
